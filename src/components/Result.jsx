import React from 'react';
import response from '../response';

const Result = () => {
    // title, link, htmlsnippet
    const data = response

    
    function resultData() {
        data.map((e) => {
            
        })
    }

    return (
        <div>
            
        </div>
    );
}

export default Result;
